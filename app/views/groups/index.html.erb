

<table class="table">
  <thead>
    <tr>
      <th>グループ名</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.name %></td>
        <td><%= link_to "詳細", group_path(group) %></td>
      </tr>
    <% end %>
  </tbody>
</table>