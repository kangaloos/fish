<%= form_with model: point do |f| %>
 <h4>Title</h4>
  <%= f.text_field :title, class: "form-control mb-2" %>

  <h4>Opinion</h4>
  <%= f.text_area :body, class: "form-control mb-2" %>

  <%= f.submit 'Create point',class: "btn btn-success btn-block my-4" %>
  
<% end %>