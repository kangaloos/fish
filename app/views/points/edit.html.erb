<% if @point.errors.any? %>
  <%= @point.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @point.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<h1>Editing point</h1>

 <%= form_with model: @point  do |f| %>

<h4>Title</h4>
 <%= f.text_field :title %>

<h4>Opinion</h4>
 <%= f.text_area :body %>

 <%= f.submit 'Update point' %>
 <%= link_to "Show", point_path(@point) %>
 <a class="back" href="/points">Back</a>



 <% end %>
